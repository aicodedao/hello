# AiCodeDao • Next-Gen Agentic Hello ✨ (v5.1)

Ứng dụng Web thế hệ mới theo phong cách **Cyber-Glassmorphism 3.0**, tích hợp **Mini AI Game ("Cyber AI Quantum Dodger")**, **Interactive 3D Multi-Polyhedra Reactor (4 Hình học không gian 3D)**, **Gemini AI Assistant Chatbot** với **Voice Assistant (Web Speech TTS)** & **Xuất lịch sử Chat (Markdown)**, **AI Agentic Core HUD Grid**, **Multi-mode Particle & Canvas Engine (4 Chế độ)**, **Web Audio API Synth Engine Pro+ (6 Presets & Game Sound Effects)** với Waveform Visualizer, **Hỗ trợ 8 Ngôn ngữ Quốc tế**, **Interactive Mini Terminal CLI v5.1**, **6 Bảng màu Neon**, **AI Wisdom Quotes Engine**, và triển khai Container **Nginx Alpine (Port 8080)** với **Cloudflare Tunnel Ingress** cho subdomain `hello.aicodedao.xyz`.

---

## 🚀 Tính năng nổi bật thế hệ v5.1

- 🎮 **Mini AI Game ("Cyber AI Quantum Dodger"):**
  - Trò chơi sinh tồn không gian mạng arcade tích hợp trực tiếp trên HTML5 Canvas.
  - Điều khiển tàu lượng tử thu thập **Quantum Orbs (+100)** và né tránh **Glitch Traps**.
  - Hệ thống **Combo Multiplier** (x1 đến x5 Overdrive) và **3 Khiên bảo vệ Lực lượng**.
  - 3 Mức độ khó: `Cơ bản (Normal AI)`, `Chiến thần (Hyper AI)`, `Godlike (Quantum Singularity)`.
  - **Gemini AI Copilot Live Commentary**: Đồng hành đưa ra bình luận chiến thuật thời gian thực.
  - Âm thanh Synthesizer độc quyền cho Game (`game-collect`, `game-hit`, `game-powerup`, `game-over`) và lưu kỷ lục vào `localStorage`.
- 🔮 **Interactive 3D Multi-Polyhedra Reactor:**
  - 4 Khối hình học 3D toán học có thể chuyển đổi linh hoạt:
    1. **Icosahedron** (Khối 20 mặt đều lượng tử)
    2. **Hypercube 4D** (Tesseract đa chiều xoay chiếu 3D)
    3. **Quantum Dodecahedron** (Khối 12 mặt đều Cyber)
    4. **Octahedron** (Kim cương 8 mặt đối xứng)
  - Ma trận xoay 3D vector perspective projection 60+ FPS tương tác cảm ứng / chuột.
- 🗣️ **Gemini AI Assistant Chatbot Nâng Cao (v5.1):**
  - **Voice Assistant (Text-to-Speech)**: Đọc to phản hồi AI bằng giọng nói tự nhiên với Web Speech API đa ngôn ngữ.
  - **Chat Transcript Export**: Xuất toàn bộ cuộc trò chuyện thành file Markdown (.md) chất lượng cao.
  - Hỗ trợ mô hình **Google Gemini 2.5 Flash**, **Gemini 2.5 Pro**, **Gemini 2.0 Flash**, và **Agentic Mock Streaming**.
  - Trình phân tích Markdown nâng cao hỗ trợ Bảng số liệu (`<table>`), Khối trích dẫn (`<blockquote>`), Tiêu đề, In đậm, và nút Copy mã nguồn.
- 🎵 **Web Audio API Synth Engine Pro+ (6 Presets & AI Sound Effects):**
  - Polyphonic Synth (`Cyber Synth`, `8-Bit Arcade`, `Zen Chime`, `ASMR Click`, `Sci-Fi Holo`, `Muted`) sinh âm thanh toán học động.
  - Hiệu ứng âm thanh tương tác AI và Game Arcade.
- 🤖 **AI Agentic Core HUD Grid:** Bảng điều khiển trực quan 4 Agent chuyên biệt (`Gemini 3.8 Flash Architect`, `Gemini 3.1 Pro Sentinel`, `TrueForge DevOps`, `Cyber HUD 3D & Mini Game Engine`) với chỉ số telemetry và trạng thái live.
- 🌌 **Multi-Mode Dynamic Canvas Engine (4 Chế độ):**
  - `Neural Web`: Mạng nơ-ron liên kết động với lực tương tác vật lý.
  - `Matrix Rain`: Cơn mưa ký tự số phong cách Cyberspace với bảng mã Katakana.
  - `Starfield`: Không gian vũ trụ 3D sao trôi chiều sâu.
  - `Warp Speed`: Vận tốc ánh sáng lượng tử siêu tốc độ.
- 🌍 **Đa ngôn ngữ thông minh (8 Ngôn ngữ):** 🇻🇳 Tiếng Việt, 🇬🇧 English, 🇯🇵 日本語, 🇫🇷 Français, 🇪🇸 Español, 🇰🇷 한국어, 🇩🇪 Deutsch, 🇨🇳 中文 kèm lời chào theo 5 khung giờ (Rạng đông, Sáng, Chiều, Tối, Khuya).
- 💻 **Interactive Mini Terminal CLI v5.1:** Giao diện dòng lệnh phong cách hacker với thanh Quick Command Chips (`/game`, `/shapes`, `/speak`, `/export`, `/status`, `/ai`, `/hud3d`, `/apikey`, `/quote`, `/theme`, `/canvas`, `/confetti`, `/agents`, `/sound`, `/ping`, `/clear`, `/share`).
- 🧊 **Cyber-Glassmorphism 3.0 & HUD Brackets:** Thẻ kính mờ với góc vát HUD Tech, viền neon xoay chuyển gradient, hiệu ứng 3D Perspective Tilt theo chuột và Ambient Glow.
- 🎨 **6 Bảng màu Neon Tuyệt đẹp:** `Cyber Aurora`, `Emerald Nexus`, `Solar Flare`, `Deep Cosmos`, `Matrix Cyber`, `Hyper Sunset`.
- 💡 **AI Wisdom Engine:** Bộ sưu tập danh ngôn kỹ thuật phần mềm và AI Agentic với tính năng sao chép và đánh dấu yêu thích (Bookmarks) lưu trên `localStorage`.
- 📱 **Smart Share & QR Code Modal:** Tích hợp Web Share API native và mã QR độ phân giải cao hỗ trợ quét từ smartphone.
- ⌨️ **Keyboard Shortcuts:** Hỗ trợ đầy đủ phím tắt (`G` Mở Mini Game, `S` Đổi hình 3D, `T` Theme, `C` Pháo hoa, `W` Đổi nền Canvas, `Q` AI Quote, `M` Âm thanh, `K` API Key, `L` Đổi ngôn ngữ, `F` Toàn màn hình, `Space` Bắn pháo hoa).
- ⚡ **Real-time Live Metrics & Battery Optimizer:** Đo FPS thời gian thực, đồng hồ múi giờ kép (Việt Nam UTC+7 & UTC), đo độ trễ Edge server, đếm tương tác, và tối ưu DPR & Tab Visibility để tiết kiệm pin.
- 🐳 **Dockerized Production Ready:** Chạy trên Nginx Alpine siêu nhẹ (< 15MB) với Gzip Compression, Security Headers (`X-Frame-Options`, `X-Content-Type-Options`) và `/healthz` endpoint trên port 8080.
- 🌐 **Cloudflare Tunnel Ingress:** Tích hợp container `hello-tunnel` kết nối an toàn với Cloudflare Zero-Trust Edge về domain `hello.aicodedao.xyz`.

---

## 📂 Cấu trúc dự án

```text
/Users/scott_ng/Develop/aicodedao/hello/
├── index.html            # DOM Cyber-Glassmorphism 3.0, Mini AI Game, 3D Core, AI Chatbot, Agent HUD & CLI
├── style.css             # CSS Tokens, Game HUD, 3D Core, Chat styling, 6 Neon Themes & Responsive
├── script.js             # Mini AI Game Engine, 3D Polyhedra Engine, Gemini Chat, Web Audio & CLI v5.1
├── nginx.conf            # Cấu hình Nginx tối ưu (Gzip, Security headers, Caching, Port 8080)
├── Dockerfile            # Multi-stage Nginx Alpine container (Port 8080)
├── docker-compose.yml    # Docker Compose orchestration (hello + hello-tunnel)
├── package.json          # Node.js project metadata & native test runner scripts
├── .dockerignore         # Loại trừ files không cần thiết khi build Docker
├── .env.example          # File mẫu cấu hình biến môi trường
├── .env                  # Cấu hình môi trường thực tế (Tunnel token)
├── .gitignore            # Bỏ qua files nhạy cảm và rác hệ thống
├── tests/
│   ├── run_tests.sh      # Comprehensive Verification Runner Script
│   ├── test_hello.test.mjs # Direct Node.js Native Test Suite (node:test & node:assert)
│   └── test_hello.py     # Python Test Suite (Static, Runtime, Ingress)
└── README.md             # Tài liệu hướng dẫn dự án
```

---

## 💻 Hướng dẫn khởi chạy & Kiểm thử

### 1. Khởi chạy với Docker Compose
Khởi chạy toàn bộ hệ thống gồm web app và Cloudflare Tunnel:
```bash
docker compose up -d --build
```

### 2. Kiểm tra Trạng thái & Logs
```bash
docker compose ps
docker compose logs --tail=50 hello-tunnel
curl -I -s "http://localhost:8080/healthz"
curl -I -s "https://hello.aicodedao.xyz"
```

### 3. Chạy Kiểm Thử Trực Tiếp từ Node.js (Direct Node Native Testing)
Chạy bộ test suite trực tiếp qua Node.js (zero-dependency native `node:test` & `node:assert`):
```bash
# Thực thi test trực tiếp qua Node
node --test tests/test_hello.test.mjs

# Hoặc qua npm test scripts
npm test
npm run test:node
```

### 4. Chạy Toàn Bộ Test Suite (Node + Python + Ingress Runner)
```bash
bash tests/run_tests.sh
# Hoặc chạy riêng Python suite
python3 tests/test_hello.py
```

---

## 🛡️ Giấy phép & Bản quyền
Phát triển bởi **AiCodeDao** © 2026.  
Website: [aicodedao.xyz](https://aicodedao.xyz)  
Domain dự án: [hello.aicodedao.xyz](https://hello.aicodedao.xyz)


